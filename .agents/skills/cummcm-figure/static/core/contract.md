# 国赛图件契约

每张图在写代码前必须声明：核心结论、读者需要看到的证据、每个面板的唯一作用、使用的数据文件与列、任何排除规则、最终插入 Word 的尺寸、导出格式和对应论文段落。

图件必须服务论证。美观不能掩盖样本量不足、统计定义缺失、数据排除、模型不稳定或图表与结论不一致。

定量图必须能追溯到 source-data 文件。任何删行、聚合、抽样和变换都要记录前后数量与理由。模型图还必须记录数据划分、随机种子或交叉验证折数、指标定义和 Baseline。
